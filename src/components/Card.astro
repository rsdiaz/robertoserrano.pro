---
interface Props {
	title: string;
	body: string;
	href: string;
}

const { href, title, body } = Astro.props;
---

<li class="link-card bg-gray-900">
	<a href={href}>
		<h2 class="font-bold">
			{title}
			<span>&rarr;</span>
		</h2>
		<p class="text-base">
			{body}
		</p>
	</a>
</li>
