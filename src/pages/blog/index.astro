---
import BigIcon from "../../components/BigIcon.astro";
import BlogCard from "../../components/BlogCard.astro";
import Footer from "../../components/Footer.astro";
import Nav from "../../components/Nav.astro";
import Layout from "../../layouts/Layout.astro";

const allPosts = await Astro.glob("./*.md");
---

<Layout title="Articulos sobre desarrollo web | Roberto Serrano">
  <BigIcon />
  <Nav />
  <div class="flex justify-center p-24">
    <div
      class="inline-block px-3 py-1 text-sm font-semibold text-indigo-100 rounded-lg md:text-center text-cn bg-[#202c47] bg-opacity-60 hover:cursor-pointer hover:bg-opacity-40"
    >
      Otro blog mas en la red sobre desarrollo web, pero este es el mio.
    </div>
  </div>
  <ul class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
    {
      allPosts.map((post) => (
        <BlogCard
          href={post.url}
          title={post.frontmatter.title}
          date={post.frontmatter.pubDate}
          tags={post.frontmatter.tags}
          image={post.frontmatter.image}
        />
      ))
    }
  </ul>
  <div class="flex justify-center mt-12">
    <a href="/" data-name="top-button" class="bg-gradient-to-r from-purple-600 to-pink-600 hover:from-pink-600 hover:to-purple-600 text-white font-bold py-2 px-4 rounded focus:ring transform transition hover:scale-105 duration-200 ease-in-out" type="button">
      ARRIBA
    </a>
  </div>
  <Footer />
</Layout>
